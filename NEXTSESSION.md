# RAMO Bot — Инструкция для следующей сессии
Дата обновления: 2026-07-22 (сессия 3)
Бот: @RamoOpsBot
Проект: C:\Users\user\Desktop\AMO
Запуск: `python -m bot.main` из корня проекта

---

## ✅ ЧТО СДЕЛАНО В СЕССИИ 3 (22.07)

### 1. SOP раскиданы по категориям
Файл: bot/utils/cache_manager.py
- **SOP кроме "Матрица зон задач"** → распределены по Регламентам (21 секция) с категориями:
  - ⏰ Правила работы (расписание, дресс-код, телефон, техника безопасности, командность)
  - 🔑 Операционные процессы (клининг, открытие, брони, доставка, счета)
  - ⚙️ Оборудование (кофемашина, пиво, музыка, освещение, проектор, кондиционер, мороженица, стиралка, летка, маркиза)
- **"Матрица зон задач"** → удалена (рассмотрим в будущем отдельно)
- Кнопка SOP в Библиотеке — **убрана**
- Кнопка Карта зала — **убрана**

### 2. Роли: разделены управляющий ↔ администратор
- **manager** (Администратор смены) — только свой функционал: посадка, доп-вопрос, счёт, брифинг, конфликты
- **head_manager** (Управляющий) — своя роль: KPI выручки, фудкост, матрица зон, сотрудники
- **security** (Охрана) — сокращена вдвое
- Добавлены 6 новых ролей (информационные):
  - 🍳 chef (Шеф-повар)
  - 🍸 bar_manager (Бар-менеджер)
  - 👑 head_manager (Управляющий — с полной информацией)
  - 🥘 cook (Повар)
  - 🧹 cleaning (Клининг)
  - 📊 pm (Проектный менеджер)

### 3. Система акций (новый модуль promos)
Файлы: bot/handlers/promos.py, bot/utils/promo_scheduler.py, bot/utils/db_connector.py (модели PromoConfig, PromoLog)

**Функции:**
- ✅ **Авто-рассылка за 10 мин до смены** (10:50 и 16:50 МСК)
- ✅ **Просмотр акций** — кнопка "🎁 Акции" в меню + команды (`акции`, `какая сегодня акция?`)
- ✅ **История оповещений** — архив по датам (доступ: admin, manager, pm)
- ✅ **Управление акциями** — вкл/выкл, редактирование описания (доступ: admin, manager, pm)
- ✅ **Расписание акций:**
  - ПН: 🍹 День коктейля
  - ВТ: 🥃 День настоек
  - СР: 🍷 Винный день
  - ЧТ: 🍺 Пивной день
  - Ежедневно: 🍽 Обеденная акция (−15%, 12–17ч)
  - OFF: 🎫 Промо-листовка (оффлайн, отключена)
- Напоминание про Stories — **убрано** (добавим позже через маркетинг-таблицу)

### 4. Меню унифицировано
Файл: bot/handlers/menu.py
- **Единое главное меню** со всеми разделами: Библиотека + Акции + Задачи + События + Передача + Инцидент + Настройки
- Для менеджеров/admin/pm: кнопка **"🔧 Панель управления"** прямо в главном меню
- **Настройки** — теперь с описанием режимов работы:
  - Режим редактора (Панель управления) ↔ Режим сотрудника (Главное меню)
  - Для менеджеров показывается роль и кнопка быстрого переключения

### 5. Регламенты теперь трёхуровневые
Файл: bot/handlers/library.py
- Регламенты → 3 категории → Разделы в категории → Контент
- Кнопка "← Назад" в деталях ведёт в категорию, потом в список категорий
- Роли: все 10 должностей в сетке 2×5
- Кнопка "Инструкция по смене" показывается только если она есть в job_instructions

### 6. Исправлен noop_handler в admin.py
- Кнопки-заголовки в редакторе чеклиста больше не вызывают двойного клика
- Handler: `@router.callback_query(F.data == "noop") async def noop_handler(callback): await callback.answer()`

---

## 📋 ТЕКУЩЕЕ СОСТОЯНИЕ БОТА

### Архитектура
```
bot/
├── handlers/
│   ├── registration.py     авто-одобрение
│   ├── menu.py             главное меню + настройки
│   ├── library.py          библиотека: чеклисты, роли, регламенты (категории), FAQ
│   ├── promos.py           акции (просмотр, управление, история) — ✅ НОВОЕ
│   ├── tasks.py            чеклисты с записью в DB
│   ├── handover.py         FSM передачи смены
│   ├── incident.py         FSM инцидентов
│   ├── events.py           брони, афиша, праздники
│   ├── admin.py            панель управления
│   └── control.py          контроль команды
├── utils/
│   ├── cache_manager.py    _SEED с контентом (роли, регламенты, акции…)
│   ├── db_connector.py     БД: User, ChecklistExecution, HandoverLog, IncidentReport, Event,
│   │                       OnboardingProgress, PromoConfig, PromoLog — ✅ НОВОЕ
│   └── promo_scheduler.py  фоновая рассылка акций — ✅ НОВОЕ
├── states/forms.py         FSM состояния (+ PromoEditState — ✅ НОВОЕ)
├── keyboards/inline_buttons.py  кнопки меню
└── main.py                 точка входа: регистрирует роутеры, инит_db, инит_promos, запускает планировщик
```

### Роли в боте (библиотека)
| Ключ | Название | КТО | Статус |
|------|----------|-----|--------|
| barman | 🍸 Бармен | Николай | Есть в регистрации |
| waiter | 🍽 Официант | Настя, Катя | Есть в регистрации |
| manager | 👔 Адм. смены | ВАКАНСИЯ | Есть в регистрации |
| security | 🛡 Охрана | - | Есть в регистрации |
| chef | 🍳 Шеф-повар | Егор | Инфо только |
| bar_manager | 🍸 Бар-менеджер | Милана | Инфо только |
| head_manager | 👑 Управляющий | ВАКАНСИЯ | Инфо только |
| cook | 🥘 Повар | - | Инфо только |
| cleaning | 🧹 Клининг | Ирина, Энеш | Инфо только |
| pm | 📊 Проект. менеджер | Константин | Инфо только |

### Модели БД (новые)
```python
PromoConfig:
  - id (int, primary key)
  - promo_key (str, unique) — "weekday_0", "lunch_15", "promo_flyer"
  - is_active (bool)
  - title, description (str, text)
  - weekday (int nullable) — 0–6 для пн–вс, None для ежедневно
  - time_start, time_end (str nullable) — "12:00", "17:00"
  - updated_at, updated_by

PromoLog:
  - id, sent_at (datetime)
  - message_text, recipients_count
```

---

## 🚀 КАК ЗАПУСТИТЬ

### Первый запуск
```powershell
cd C:\Users\user\Desktop\AMO
python -m bot.main
```

### Перезапуск (если поменялись данные)
```powershell
# 1. Остановить
Get-Process | Where-Object {$_.Name -like "python*"} | Stop-Process -Force

# 2. Если поменялась _SEED — удалить соответствующие кэши:
Remove-Item "C:\Users\user\Desktop\AMO\data\cache\roles.json" -Force
Remove-Item "C:\Users\user\Desktop\AMO\data\cache\regulations.json" -Force
Remove-Item "C:\Users\user\Desktop\AMO\data\cache\sop.json" -Force

# 3. Запустить
cd C:\Users\user\Desktop\AMO
python -m bot.main
```

---

## 📝 ДОСТУПНЫЕ ДИРЕКТИВЫ ДЛЯ CLAUDE

**В памяти (C:\Users\user\.claude\projects\C--Users-user-Desktop-AMO\memory\):**
- `feedback.md` — Не срезать контент SOP, давать полные описания, форматировать по ролям
- `user_profile.md` — Владелец RAMO, работает в автономном режиме, проверяет в Telegram
- `project_ramo.md` — Контекст проекта (опционально)

**Повтори инструкции в новой сессии:**
1. **Не срезать контент** — полные чеклисты, KPI, red lines
2. **Работать автономно** — не спрашивать, делать, пока есть токены
3. **Форматировать через HTML** — `<b>`, `<i>`, списки, структура
4. **Проверять визуально** — изменения важны, проверь в боте перед тем как сказать готово
5. **Перезапускать бота** после любых changes в `*.py` файлах

---

## 🔗 ССЫЛКА НА СКАЧИВАНИЕ ПРОЕКТА

**Архив проекта:**  
Находится на диске машины: `C:\Users\user\Desktop\AMO\`

Для скачивания за пределы машины:
- Используй облако (Google Drive, Яндекс.Диск) или git-репозиторий
- Или отправь архив по почте vorobyov.kostya.ok@gmail.com

**Сейчас:**
- БД: `data/db.sqlite` (SQLite, ~50KB)
- Кэш: `data/cache/*.json` (~пересоздаются автоматически)
- Исходники: все в `bot/` и `bot/handlers/`, `bot/utils/`
- .env: `BOT_TOKEN`, `ADMIN_TELEGRAM_ID=484975342`

---

## 🎯 СЛЕДУЮЩИЕ ЗАДАЧИ

- [ ] Google Sheets — таблица графика смен (требует Google API)
- [ ] Маркетинг-таблица для Stories (источник контента для SMM-напоминаний)
- [ ] Хостинг на VPS (бот сейчас на локальном ПК)
- [ ] Роль администратора в регистрации (если нужны отдельные права от manager)
