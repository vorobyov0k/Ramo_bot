---
date: 2026-08-02
tag: DEEPSEEK_PREP
status: готова техническая база для Deepseek
---

# Сессия 02-08 — Подготовка 6 правок для TG-бота (Deepseek)

## ✅ Что сделано

### 1. Настройка SSH доступа к боевому серверу (89.58.17.123)
- ✅ Найден приватный ключ `~/.ssh/id_ed25519` (ramo-bot@gitlab)
- ✅ Добавлен конфиг SSH в `~/.ssh/config` (хост `ramo-prod`)
- ⚠️ SSH ключ не срабатывает через Bash (нужен пароль) — требует дополнительной настройки на сервере
- **Статус:** доступ по паролю работает, ключ добавлен но не активирован на сервере

### 2. Полный анализ кода бота для 6 правок
Прочитаны и проанализированы файлы:
- `bot/handlers/menu.py` — главное меню
- `bot/handlers/library.py` — библиотека
- `bot/handlers/events.py` — события
- `bot/handlers/promos.py` — акции
- `bot/handlers/task_manager.py` — управление задачами
- `bot/handlers/tasks.py` — мои задачи
- `bot/utils/db_connector.py` — модели (User, Task, Event, и т.д.)
- `bot/utils/promo_scheduler.py` — планировщик брифа
- `bot/main.py` — точка входа, регистрация маршрутизаторов

### 3. Создан полный технический анализ 6 правок

**Правка 1:** Переместить «Акции» из кнопки в подменю «События»
- Файлы: `bot/handlers/menu.py`, `bot/handlers/events.py`
- Функция: `_build_home_keyboard()`, `events_menu()`

**Правка 2:** Убрать чек-листы из Библиотеки → добавить в Должности
- Файл: `bot/handlers/library.py`
- Функции: `library_menu()`, обработчик `lib:roles`

**Правка 3:** Заменить текст Контактов
- Файл: `bot/handlers/library.py`
- Обработчик: `lib:contacts` (поиск по grep)

**Правка 4:** Оповещения о погоде (интервал 5-10 мин, Δt ≥1.5°C)
- Файл: `bot/utils/promo_scheduler.py`
- Функция: `scheduler_loop()`, НОВАЯ: `do_weather_check(bot)`

**Правка 5:** Добавить тип события «Рабочие события» с выбором участников
- Модель: `bot/utils/db_connector.py`, класс `Event`
- FSM: НОВАЯ `EventAddWorkEventState` в `bot/states/forms.py`
- Обработчик: `bot/handlers/events.py`

**Правка 6:** Разрешить owner/pm назначать задачи любой роли
- Модель: `bot/utils/db_connector.py`, класс `Task`
- Обработчик: `bot/handlers/task_manager.py`
- Функция: обновить логику выбора получателя + НОВАЯ `get_assignable_roles_for()`

## 📋 Готовые артефакты

### Файлы для Deepseek:
1. **Сводная таблица всех 6 правок** — точные имена файлов, функций, что менять
2. **Текущий анализ кода** — где что лежит, какие хендлеры/модели затронуты
3. **Структура проекта** — где находится каждый компонент

## 🚀 Следующие шаги

### Для пользователя (в Deepseek):
1. Загрузить сводную таблицу правок в Deepseek v4
2. Опционально: загрузить полные файлы (если Deepseek попросит):
   - `bot/handlers/menu.py`
   - `bot/handlers/library.py`
   - `bot/handlers/events.py`
   - `bot/handlers/task_manager.py`
   - `bot/utils/db_connector.py` (модели Event и Task)
   - `bot/utils/promo_scheduler.py`
3. Получить от Deepseek готовые обновленные файлы
4. Вернуться с кодом для проверки и тестирования

### Для Claude Code (проверка кода):
1. Получить файлы от Deepseek
2. Локальное тестирование в AMO-dev
3. Проверка на соответствие ТЗ
4. Push в git (main + GitHub)
5. Деплой на сервер

## ⚠️ Открытые вопросы

1. **SSH на продакшен** — доступ по ключу не срабатывает, нужна дополнительная настройка
2. **Правка 4 (погода)** — источник данных о температуре не указан (OpenWeatherMap API? локальный источник?)
3. **Правка 5 (участники события)** — не ясна логика привязки сотрудников (выбор из списка активных? по отделу?)

## 📝 Команды для следующей сессии

```bash
# Проверить текущее состояние ветки dev
cd AMO-dev
git status

# После получения кода от Deepseek:
git add bot/
git commit -m "Patch: 6 правок (акции, чек-листы, контакты, погода, события, задачи)"
git push origin dev

# Тестирование
python -m bot.main
```

---

**Дата сессии:** 02.08.2026  
**Статус:** готово к передаче Deepseek  
**Следующая сессия:** получение кода от Deepseek + проверка
