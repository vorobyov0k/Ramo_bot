---
date: 2026-08-05
tag: BAR_MENU_CARDS_UPDATE
status: 🔄 готово локально, деплой на прод отложен (ждём доп. правки в другой сессии)
---

# Сессия 05-08 — Обновление карточек бара (состав + вкусовой профиль)

## Задача

Обновить карточки напитков в боте по данным из `C:\Users\user\Downloads\барное меню август Рамо.md` (14 категорий, 78 позиций): подтянуть ингредиенты+граммовку и добавить вкусовой профиль, не трогая структуру категорий. Целевой вид карточки: фото → название (жирным) → описание (вкус) → состав (таблицей) → цена (жирным).

## Что сделано (всё локально, НЕ выгружено на прод)

1. **`scripts/match_bar_menu.py`** (новый) — матчинг 72 из 77 напитков БД с новой таблицей по названию/категории. 5 сирот в БД без пары в новой таблице — **решение отложено, показать Тарасу/бару**:
   - id=31 «Яблоко / карамель / ваниль» (Лимонады)
   - id=41 «Продрай» (Игристые вина)
   - id=59 «Персиковый Хайбол», id=60 «Красный Гимлет», id=65 «Утка спешл» (Коктейли)
2. **`scripts/update_bar_menu_composition.py`** (новый, dry-run/--apply) — применён локально к `data/menu.db` (бэкап: `data/menu.db.bak-20260805171548`). Обновлено 72 напитка:
   - Состав (`composition`) — таблица «ингредиент — граммовка» только там, где в источнике реально есть цифры (кофе, какао, айс-ти спешл, ~14 позиций). Остальные 58 совпавших позиций — состав очищен (был мусор: цена/объём вместо состава), поле оставлено пустым для ручного заполнения персоналом.
   - Описание (`description`) — вкусовой профиль от bar-субагента, для всех 72 позиций.
3. **`bot/handlers/library.py`**, `drink_detail` — новый порядок полей: имя (жирным) → описание → состав (`<pre>`-таблица) → цена (жирным) → теги. Проверено рендером в консоли, в лимит caption 1024 символа укладывается с запасом.
4. **Админ-редактор** (новое, `bot/handlers/admin.py` + `bot/states/forms.py`, роли admin/pm/owner) — на карточке напитка в разделе «📸 Фото меню» добавлены кнопки «✏️ Редактировать состав» и «✏️ Редактировать описание» (FSM `MenuTextEditState`), плюс `update_drink_composition`/`update_drink_description` в `bot/utils/menu_db.py`. Нужно персоналу, чтобы дозаполнить состав там, где в таблице не было граммовки.

## Открытые вопросы / что осталось

- **2 позиции с изменённым рецептом под тем же названием** (id=63 «Белый Рамо», id=64 «Клубничный спритц») — по решению взят новый состав/вкус из таблицы (считаем, что рецепт обновился), но состав для них остаётся пустым как для всех коктейлей (граммовок в источнике нет) — вкус уже обновлён.
- **5 сирот выше** — ждут решения Тараса/бара: убрать из меню или оставить как есть.
- **Фото карточек** — 0 из 77 напитков имеют `photo_id` (стёрты в июльском ребилде, вне рамок этой задачи). Порядок «фото → имя → …» в коде уже готов, просто нечего показывать, пока фото не перезалиты через админку.

## Следующая сессия

Пользователь попросил **не деплоить пока** — будут добавлены ещё правки в другой сессии, потом задеплоить всё вместе. План деплоя (готов, просто не выполнен):

1. Бэкап прод-БД: `ssh root@89.58.17.123 "cp /opt/ramo_bot/data/menu.db /opt/ramo_bot/data/menu.db.bak-$(date +%Y%m%d%H%M%S)"`
2. `scp scripts/update_bar_menu_composition.py root@89.58.17.123:/opt/ramo_bot/scripts/` → `ssh ... "cd /opt/ramo_bot && venv/bin/python scripts/update_bar_menu_composition.py --apply"`
3. `scp bot/handlers/library.py root@89.58.17.123:/opt/ramo_bot/bot/handlers/library.py`
4. `scp bot/handlers/admin.py bot/states/forms.py bot/utils/menu_db.py root@89.58.17.123:/opt/ramo_bot/bot/...` (соответствующие пути)
5. `ssh root@89.58.17.123 "sudo systemctl restart ramo-bot.service"` (НЕ `python main.py &`)
6. Проверка: `systemctl status`, `journalctl -n 50`, карточка в реальном Telegram.
